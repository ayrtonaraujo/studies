#2. Dado o tamanho do lado de um quadrado, calcular
# a área e o perímetro do mesmo.

medida = input("Altura ou Largura:")

area = float(medida) * 2
pert = float(medida) * 4

print ('Área:', float(area), "m²")
print ("Perímetro:", float(pert), "m")